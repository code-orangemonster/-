        int l=0,r=0;
        int res=0;
        while(r<n){
            //  todo ： 右指针 状态加入

            // todo：不符合状态，左指针右移，直到满足状态
            while(){
                // todo ： 左指针右移状态改变
               
           
                l++;
            }
            // 左指针移动到符合状态，求解答案
            res=max(res,r-l+1);
            // 右指针右移
            r++;
        }